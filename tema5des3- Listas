# Lista original con elementos duplicados
lista_original = [1, 3, 4, 2, 5, 1, 7, 6, 4, 4, 8, 7]

# Lista original a mostrar.
print("Lista original:", lista_original)

# Se utiliza set() para convertir la lista en un conjunto, lo que automáticamente elimina los duplicados.
# Luego, se convierte de nuevo a una lista con list().
lista_sin_duplicados = list(set(lista_original))

# Mostrar la lista sin duplicados
print("Lista sin duplicados:", lista_sin_duplicados)
